import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    filterBlock: {
        flexDirection: 'row',
        marginBottom: 40,
        width: '100%',
        margin: 20,
    },
});

export default styles;